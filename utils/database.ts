/*

import { Database } from '../utils/Database';

const result = await Database.executeQuery(
  "SELECT * FROM Appointments WHERE PatientName = 'Akalya'"
);

expect(result.length).toBeGreaterThan(0);

*/
